<div class="project" data-image="<%= post.image %>">
  <div class="project__image" style="background-image: url(<%= post.image %>)">
  </div>
  <div class="project__gradient"></div>
  <!-- <%= link_to post_path(post.nice_url), class: "project__image", data: { image: "#{post.image}" }, style: "background-image: url(#{post.image})" do %>
     <div class="project__overlay">
      <i class="project__icon material-icons">search</i>
    </div> -->
  <!-- <% end %> -->
  <!-- <div class="project__content">
    <span class="project__name"><%= post.title %></span>
    <% if post.user_name %>
      <span class="project__author"><%= post.user_name %></span>
    <% end %>
    <%= link_to "€" + post.price, post_path(post.nice_url), class: "project__price" %>
  </div> -->
</div>
